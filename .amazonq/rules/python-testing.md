## Python testing (Priority: Medium)

- Add `pytest` with `uv add pytest` if missing
- Use pytest markers for implementation phases
- Run `pytest` to validate changes
- All tests must pass before marking phases complete
